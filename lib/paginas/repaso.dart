import 'package:flutter/material.dart';

class RepasoPage extends StatelessWidget {
  const RepasoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: const Center(
        child: const Text('Pagina Repaso'),
      ),
    );
  }
}
